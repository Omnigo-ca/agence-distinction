# Auto detect text files and perform LF normalization
* text=auto

# Vidéos volumineuses — Git LFS (WMV/M4V non utilisés en hero web)
*.wmv filter=lfs diff=lfs merge=lfs -text
*.m4v filter=lfs diff=lfs merge=lfs -text
